import * as React from "react";
import styled from "styled-components";

function CreateTeam({ className, ...props }) {
  return (
    <section className={className} {...props}>
      create team section
    </section>
  );
}

export { CreateTeam };
